# ============================================================================
# Custom Build Configuration Overrides
#
# Intentionally empty: this custom build currently matches stock
# QGroundControl (branding, icons, firmware plugin factories).
# Add overrides here as customizations land — see
# custom-example/cmake/CustomOverrides.cmake for the full template.
#
# NOTE: overrides use CACHE FORCE, so after removing one, wipe the build
# directory (fresh configure) to drop the stale cached value.
# ============================================================================
