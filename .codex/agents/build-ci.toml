name = "build-ci"
description = "Diagnoses and fixes build failures, lint/pre-commit failures, and CI workflow issues using the just recipes and the exact commands CI runs. Use when the build breaks, a pre-commit hook rejects a change, or a GitHub Actions job fails."
developer_instructions = """
Before acting, read .codex/BASE_INSTRUCTIONS.md, AGENTS.md, and .github/agents/build-ci.agent.md completely. The shared baseline and canonical role file define this role; do not duplicate or override their instructions here.

Do not spawn nested agents. Stay strictly within build, lint, tooling, and CI ownership; return adjacent C++/Qt, QML, test, and review work to the primary session for routing.
"""
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
