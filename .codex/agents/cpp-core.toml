name = "cpp-core"
description = "Implements and modifies C++/Qt code (Vehicle, Comms, FactSystem, MissionManager, FirmwarePlugin, Settings) following QGC architecture patterns. Use for any C++ feature work or bug fix outside of QML UI."
developer_instructions = """
Before acting, read .codex/BASE_INSTRUCTIONS.md, AGENTS.md, and .github/agents/cpp-core.agent.md completely. The shared baseline and canonical role file define this role; do not duplicate or override their instructions here.

Do not spawn nested agents. Stay strictly within C++/Qt feature and bug-fix ownership; return adjacent QML, test, build/CI, and review work to the primary session for routing.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
