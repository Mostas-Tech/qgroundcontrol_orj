name = "qml-ui"
description = "Builds and modifies QML UI (FlyView, PlanView, QmlControls, AutoPilotPlugins setup pages) using QGC's reusable controls and screen-scaling conventions. Use for any user-facing UI change."
developer_instructions = """
Before acting, read .codex/BASE_INSTRUCTIONS.md, AGENTS.md, and .github/agents/qml-ui.agent.md completely. The shared baseline and canonical role file define this role; do not duplicate or override their instructions here.

Do not spawn nested agents. Stay strictly within user-facing QML ownership; return adjacent C++/Qt, test, build/CI, and review work to the primary session for routing.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
