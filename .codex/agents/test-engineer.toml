name = "test-engineer"
description = "Writes, extends, and repairs unit/integration tests using the QGC test framework (UnitTest base classes, MockLink, MultiSignalSpy, CTest labels). Use after behavior changes or when tests fail/flake."
developer_instructions = """
Before acting, read .codex/BASE_INSTRUCTIONS.md, AGENTS.md, and .github/agents/test-engineer.agent.md completely. The shared baseline and canonical role file define this role; do not duplicate or override their instructions here.

Do not spawn nested agents. Stay strictly within test ownership; return adjacent C++/Qt, QML, build/CI, and review work to the primary session for routing.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
